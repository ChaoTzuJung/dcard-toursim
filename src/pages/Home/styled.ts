import styled, { css } from 'styled-components';

export const S = {
    Wapper: styled.div`
        max-width: var(--maxWidth);
        margin: 0 auto;
        display: flex;

        & .sidebar {
            margin-right: 42px;
        }

        & .list-container {

        }
    `,

}